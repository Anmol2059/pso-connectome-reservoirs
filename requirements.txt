# Core scientific stack
numpy>=1.23
scipy>=1.9
matplotlib>=3.6
networkx>=2.8
pandas>=1.5
seaborn>=0.12

# Reservoir computing
reservoirpy>=0.3
scikit-learn>=1.1
bctpy>=0.6

# Swarm and evolutionary optimisation
pyswarms>=1.3
mealpy>=3.0       # 150+ meta-heuristics incl. Grey Wolf Optimiser

# GPU acceleration (optional — CPU fallback if not installed)
# On NVIDIA: pip install torch  (or pip install torch --index-url https://download.pytorch.org/whl/cu121)
# On Apple Silicon M1/M2/M3: pip install torch  (MPS backend included automatically)
torch>=2.0

# Jupyter
jupyter>=1.0
ipykernel>=6.0

# gym compatibility shim (conn2res dependency)
gym==0.26.2

# conn2res itself — install manually from source:
#   git clone https://github.com/netneurolab/conn2res.git
#   cd conn2res && pip install --no-deps .
# Note: neurogym + torch are NOT required;
# we bypass conn2res.tasks and implement MC + Lorenz directly.
